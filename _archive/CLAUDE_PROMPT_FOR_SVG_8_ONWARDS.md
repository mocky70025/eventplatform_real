# Claude用プロンプト：8番以降のSVGデザイン生成

## 背景情報

出店者アプリ（モバイル版）のSVGデザインを作成しています。1-7番までのSVGは完成しており、8番以降のSVGデザインを生成してほしいです。

## デザインシステム（カラーパレット）

以下のカラーパレットを厳密に使用してください：

- **Primary**: `#FF8A5C`
- **Secondary**: `#5DABA8`（出店者アプリのメインカラー）
- **Background**: `#FFF5F0`
- **Shadow**: 8% opacity black（`rgba(0, 0, 0, 0.08)`）

### その他の使用色

- **テキスト色（メイン）**: `#2C3E50`
- **テキスト色（サブ）**: `#6C757D`
- **ボーダー色**: `#E9ECEF`
- **白**: `#FFFFFF`
- **グレー（ナビゲーション非アクティブ）**: `#666666`
- **LINEボタン**: `#06C755`
- **Googleボタン**: `#FFFFFF`（白背景、ボーダー付き）

## 画面サイズ

- **幅**: 393px
- **高さ**: 852px
- **ViewBox**: `0 0 393 852`

## 既存のSVGデザイン（1-7番）

### 1️⃣ WelcomeScreen（初期状態 - ログインタブ選択）- Mobile版

**説明**: 出店者アプリのログイン・新規登録の初期画面。上部タブで切り替え可能。ログインタブが選択された状態。

**特徴**:
- 背景色: `#FFF5F0`
- 中央に白いカード（角丸、シャドウ付き）
- ロゴエリア（`#5DABA8`背景）
- 「ログイン」と「新規登録」のタブ（アクティブタブは`#5DABA8`の下線）
- LINE、Google、メールアドレスのログインボタン
- ボタンはシャドウ付き（`filter`要素を使用）

### 1️⃣-2️⃣ WelcomeScreen（初期状態 - 新規登録タブ選択）- Mobile版

**説明**: 新規登録タブが選択された状態。基本的な構造は1️⃣と同じ。

### 3️⃣ WelcomeScreen（ログイン - メールアドレス入力）- Mobile版

**説明**: メールアドレスでログインする画面。

**特徴**:
- メールアドレスとパスワードの入力フィールド
- 入力フィールドは白背景、`#E9ECEF`ボーダー、フォーカス時は`#5DABA8`ボーダー
- ログインボタンは`#5DABA8`背景
- 「別の方法でログイン」ボタン

### 4️⃣ WelcomeScreen（新規登録 - メールアドレス入力）- Mobile版

**説明**: メールアドレスで新規登録する画面。

**特徴**:
- メールアドレスのみの入力フィールド
- 新規登録ボタンは`#5DABA8`背景
- 「別の方法で新規登録」ボタン

### 4️⃣-2️⃣ EmailSent（メール送信完了）- Mobile版

**説明**: メール送信完了画面。確認メールを送信したことを伝える。

**特徴**:
- 中央にメールアイコン（`#5DABA8`背景の円形エリア）
- 「メールを送信しました」のタイトル
- 説明文（複数行）
- メールが届かない場合の案内ボックス（`#F8F9FA`背景）

### 5️⃣ RegistrationForm（ステップ1 - 基本情報入力 + 書類アップロード）- Mobile版

**説明**: 出店者登録フォームのステップ1。基本情報（名前、性別、年齢、電話番号、メールアドレス、ジャンル）の入力と、営業許可証、車検証、自動車検査証、PL保険、火器類配置図のアップロードを行う画面。

**特徴**:
- 上部にプログレスインジケーター（3ステップ）
- ステップ1の円は`#5DABA8`背景
- 入力フィールドは白背景、`#E9ECEF`ボーダー
- ファイルアップロードエリア
- 「次へ」ボタンは`#5DABA8`背景

### 6️⃣ RegistrationForm（ステップ2 - 情報確認）- Mobile版

**説明**: 出店者登録フォームのステップ2。登録した情報があっているかどうかを確認する画面。

**特徴**:
- プログレスインジケーターでステップ2がアクティブ
- 入力した情報の確認リスト
- 「修正する」と「次へ」ボタン

### 7️⃣ RegistrationForm（ステップ3 - 登録完了）- Mobile版

**説明**: 出店者登録フォームのステップ3。登録完了を表示する画面。

**特徴**:
- プログレスインジケーターでステップ3がアクティブ
- 完了アイコン（チェックマークなど）
- 「登録が完了しました」のメッセージ
- 「マイページへ」ボタン

## 8番以降の画面リスト

以下の画面のSVGデザインを生成してください：

### 8️⃣ EventList（イベント一覧 - マイホーム）- Mobile版

**説明**: ログイン後のマイホーム画面。イベント一覧が表示され、下部にナビゲーションバー（イベント、登録情報、申し込み、通知）が表示される。

**要件**:
- 背景色: `#FFF5F0`
- 上部にヘッダー（白背景、シャドウ付き）
- 検索バー（白背景、角丸、ボーダー）
- フィルタータブ（「すべて」「開催中」「予定」）- アクティブは`#5DABA8`背景
- イベントカード（複数、白背景、角丸、ボーダー、画像プレースホルダー、イベント名、日付、場所）
- 下部にナビゲーションバー（白背景、4つのアイコンとラベル）
  - イベント（アクティブ: `#06C755`または`#5DABA8`）
  - 登録情報（非アクティブ: `#666666`）
  - 申し込み（非アクティブ: `#666666`）
  - 通知（非アクティブ: `#666666`）

### 9️⃣ EventList（検索画面）- Mobile版

**説明**: イベント検索画面。キーワード、期間、都道府県、市区町村で検索できる。

**要件**:
- 背景色: `#FFF5F0`
- 上部にヘッダー（白背景、シャドウ付き）
- 「イベント検索」タイトル
- 検索フォーム（白背景のカード内）
  - キーワード入力フィールド
  - 開催期間（開始日）入力フィールド
  - 開催期間（終了日）入力フィールド
  - 都道府県選択フィールド
  - 市区町村入力フィールド
- 「検索する」ボタン（`#5DABA8`背景、シャドウ付き）

### 1️⃣0️⃣ EventDetail（イベント詳細）- Mobile版

**説明**: イベントの詳細画面。イベントの詳細情報、申し込みボタンが表示される。

**要件**:
- 背景色: `#FFF5F0`
- イベント画像（上部、全幅）
- イベント名、日付、場所、説明など
- 「このイベントに申し込む」ボタン（`#5DABA8`背景）

### 1️⃣1️⃣ EventCard（イベントカード）- Mobile版

**説明**: イベントカードのコンポーネント。リスト内で使用される。

**要件**:
- 白背景、角丸、ボーダー
- 画像エリア（左側）
- イベント名、日付、場所（右側）
- シャドウ付き

### 1️⃣2️⃣ ExhibitorProfile（プロフィール表示）- Mobile版

**説明**: 出店者のプロフィール表示画面。

**要件**:
- 背景色: `#FFF5F0`
- プロフィール情報（名前、メールアドレス、電話番号など）
- 「編集する」ボタン

### 1️⃣3️⃣ ExhibitorProfile（プロフィール編集）- Mobile版

**説明**: 出店者のプロフィール編集画面。

**要件**:
- 背景色: `#FFF5F0`
- 編集可能な入力フィールド
- 「保存する」ボタン（`#5DABA8`背景）

### 1️⃣4️⃣ ApplicationManagement（申し込み管理）- Mobile版

**説明**: イベントへの申し込みを管理する画面。

**要件**:
- 背景色: `#FFF5F0`
- 申し込み一覧（ステータス別に表示）
- 各申し込みカード（イベント名、ステータス、日付など）

### 1️⃣5️⃣ NotificationBox（通知一覧）- Mobile版

**説明**: 通知一覧画面。

**要件**:
- 背景色: `#FFF5F0`
- 通知リスト（未読/既読の区別）
- 各通知カード（タイトル、内容、日時）

### 1️⃣6️⃣ EmailConfirmationBanner（メール確認バナー）- Mobile版

**説明**: メール確認が必要な場合に表示されるバナー。

**要件**:
- バナー形式（上部または下部）
- 警告色または`#5DABA8`背景
- 「メールを確認してください」メッセージ

### 1️⃣7️⃣ EmailConfirmationPending（メール確認待ち画面）- Mobile版

**説明**: メール確認待ちの専用画面。

**要件**:
- 背景色: `#FFF5F0`
- メールアイコン
- 「メールを確認してください」メッセージ
- 再送信ボタン

## SVG作成のガイドライン

1. **構造**:
   - `<svg>`タグで開始
   - `<g clip-path="url(#clip0_X)">`でコンテンツをラップ
   - `<defs>`セクションでフィルターとクリップパスを定義

2. **シャドウ**:
   - カードやボタンには`filter`要素を使用
   - `feOffset dy="8"`、`feGaussianBlur stdDeviation="16"`、`feColorMatrix`で8% opacity blackを設定

3. **角丸**:
   - `rx`属性を使用（通常8px、カードは12px、ボタンは12px）

4. **テキスト**:
   - `font-family="Arial"`を使用
   - フォントサイズ: タイトル18px、本文14px、サブテキスト12px
   - フォントウェイト: タイトル600、本文400

5. **アイコン**:
   - SVGパスで描画するか、シンプルな図形で表現
   - ナビゲーションアイコンは24x24px程度

6. **一貫性**:
   - 既存の1-7番のデザインパターンを踏襲
   - 同じスタイルのボタン、入力フィールド、カードを使用

## 出力形式

各画面について、以下の形式でSVGコードを出力してください：

```markdown
## X️⃣ [画面名] - Mobile版

**説明**: [画面の説明]

<svg width="393" height="852" viewBox="0 0 393 852" fill="none" xmlns="http://www.w3.org/2000/svg">
[SVGコード]
</svg>

---
```

## 注意事項

- 8番は既にSVGが存在するため、9番以降を生成してください
- すべての色はデザインシステムに従ってください
- モバイルファーストのデザインを意識してください
- 実用的で使いやすいUIを心がけてください
- 既存のデザインパターンとの一貫性を保ってください

以上を踏まえて、9番以降のSVGデザインを生成してください。






